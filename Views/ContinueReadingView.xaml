<UserControl x:Class="MSCS.Views.ContinueReadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MSCS.ViewModels"
             Height="Auto" Width="Auto">

    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBackground" Color="#1A2130"/>
        <SolidColorBrush x:Key="CardBorder" Color="#262F42"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#7C9CFF"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#A8B3C5"/>

        <Style x:Key="EntryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid>
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasEntries}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <ListView ItemsSource="{Binding Entries}"
                      BorderThickness="0"
                      Background="Transparent"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <EventSetter Event="PreviewMouseLeftButtonUp" Handler="OnEntryClicked"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ContinueReadingEntryViewModel}">
                        <Border Style="{StaticResource EntryCardStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding MangaTitle}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="White"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding ChapterDisplay}"
                                               Margin="0,6,0,0"
                                               Foreground="{StaticResource MutedBrush}"/>
                                    <TextBlock Text="{Binding ProgressSummary}"
                                               Margin="0,2,0,0"
                                               Foreground="{StaticResource MutedBrush}"/>
                                    <TextBlock Text="{Binding LastUpdatedDisplay}"
                                               Margin="0,2,0,0"
                                               Foreground="{StaticResource MutedBrush}"/>
                                </StackPanel>

                                <Button Grid.Row="0"
                                        Grid.Column="1"
                                        Margin="12,0,0,0"
                                        Padding="10,4"
                                        HorizontalAlignment="Right"
                                        VerticalAlignment="Top"
                                        Content="Remove"
                                        Command="{Binding RemoveCommand}"
                                        CommandParameter="{Binding}"/>

                                <ProgressBar Grid.Row="1"
                                             Grid.Column="0"
                                             Grid.ColumnSpan="2"
                                             Margin="0,12,0,0"
                                             Height="6"
                                             Value="{Binding ScrollPercentage, Mode=OneWay}"
                                             Maximum="100"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <TextBlock Text="Start reading a chapter to see it here."
                   Foreground="{StaticResource MutedBrush}"
                   FontSize="14"
                   FontStyle="Italic"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasEntries}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
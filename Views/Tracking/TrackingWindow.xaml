<Window x:Class="MSCS.Views.TrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MSCS.ViewModels"
        xmlns:views="clr-namespace:MSCS.Views"
        Height="640"
        Width="420"
        Background="{DynamicResource SurfaceLow}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AniListTrackingViewModel}">
            <views:AniListTrackingView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MyAnimeListTrackingViewModel}">
            <views:MyAnimeListTrackingView />
        </DataTemplate>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource Surface}" />
            <Setter Property="Foreground" Value="{DynamicResource SidebarText}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorder}" />
            <Setter Property="Padding" Value="6,4" />
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource SidebarText}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Background" Value="{DynamicResource SurfaceLow}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SurfaceBorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource Accent}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentForeground}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Margin="12" Background="{DynamicResource SurfaceLow}" BorderBrush="{DynamicResource SurfaceBorderSoft}" BorderThickness="1" CornerRadius="10">
        <TabControl ItemsSource="{Binding Providers}"
                    SelectedItem="{Binding SelectedProvider}"
                    Margin="-1">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}"
                               Foreground="{DynamicResource SidebarText}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Opacity="1" />
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </Border>
</Window>
<UserControl x:Class="MSCS.Views.MangaListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MSCS.Models"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <Style x:Key="ResultItemContainerStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{DynamicResource Surface}"
                                BorderBrush="{DynamicResource SurfaceBorderSoft}"
                                BorderThickness="1"
                                CornerRadius="12">
                            <Border Padding="16">
                                <ContentPresenter Margin="0"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SurfaceAlt}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SurfaceAlt}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EmptyStatePanelStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Vertical"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,32,0,0"/>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <!-- Search Bar -->
        <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="0,0,0,20" HorizontalAlignment="Center">
            <Border Style="{StaticResource SurfaceSectionStyle}" Padding="16" CornerRadius="16" Effect="{x:Null}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="240"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              Style="{StaticResource InputComboBoxStyle}"
                              ItemsSource="{Binding AvailableSources}"
                              DisplayMemberPath="DisplayName"
                              SelectedValuePath="Key"
                              SelectedValue="{Binding SelectedSourceKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Foreground="Black"/>

                    <TextBox Grid.Column="2"
                             Style="{StaticResource InputTextBoxStyle}"
                             MinWidth="260"
                             Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Search by manga title"/>

                    <Button Grid.Column="4"
                            Content="Search Manga"
                            Style="{StaticResource AccentButtonStyle}"
                            MinWidth="150"
                            Command="{Binding SearchCommand}"/>
                </Grid>
            </Border>
        </DockPanel>

        <!-- Manga Results List -->
        <Grid>
            <ListView x:Name="ResultList"
                      BorderThickness="0"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      Background="Transparent"
                      ItemsSource="{Binding MangaResults}"
                      SelectedItem="{Binding SelectedManga, Mode=TwoWay}"
                      ItemContainerStyle="{StaticResource ResultItemContainerStyle}"
                      SelectionChanged="ResultList_SelectionChanged">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:Manga}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Width="86" Height="120" CornerRadius="10" ClipToBounds="True">
                                <Image Source="{Binding CoverImageUrl}"
                                       Stretch="UniformToFill"
                                       SnapsToDevicePixels="True"/>
                            </Border>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold"
                                           FontSize="18"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource Fg}"/>
                                <TextBlock Text="{Binding Url}"
                                           FontSize="14"
                                           Foreground="{DynamicResource FgMuted}"
                                           TextWrapping="Wrap"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Style="{StaticResource EmptyStatePanelStyle}"
                        Visibility="{Binding ElementName=ResultList, Path=Items.IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text=""
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="32"
                           Foreground="{DynamicResource FgSubtle}"
                           Opacity="0.6"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Start searching to discover new manga."
                           Foreground="{DynamicResource FgMuted}"
                           FontSize="14"
                           Margin="0,12,0,0"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="320"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
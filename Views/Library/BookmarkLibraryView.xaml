<UserControl x:Class="MSCS.Views.BookmarkLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MSCS.ViewModels"
             Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="BookmarkItemContainerStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,14"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{DynamicResource Surface}"
                                BorderBrush="{DynamicResource SurfaceBorderSoft}"
                                BorderThickness="1"
                                CornerRadius="12">
                            <Border Padding="16">
                                <ContentPresenter />
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SurfaceAlt}"/>
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource SurfaceBorder}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EmptyStatePanelStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Vertical"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,32,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="0"
                    Margin="0,0,0,16"
                    VerticalAlignment="Center">
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasBookmarks}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBox Width="320"
                     Style="{StaticResource InputTextBoxStyle}"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Filter bookmarks by title"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <ListView x:Name="BookmarkList"
                      ItemsSource="{Binding Bookmarks}"
                      SelectedItem="{Binding SelectedBookmark, Mode=TwoWay}"
                      BorderThickness="0"
                      Background="Transparent"
                      ItemContainerStyle="{StaticResource BookmarkItemContainerStyle}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:BookmarkItemViewModel}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="72"
                                    Height="96"
                                    CornerRadius="10"
                                    Background="{DynamicResource SurfaceAlt}"
                                    ClipToBounds="True">
                                <Grid>
                                    <Image Source="{Binding CoverImageUrl}"
                                           Stretch="UniformToFill">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasCover}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    <StackPanel HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Visibility="Collapsed">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasCover}" Value="False">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text=""
                                                   FontFamily="Segoe MDL2 Assets"
                                                   FontSize="24"
                                                   Foreground="{DynamicResource FgSubtle}"/>
                                        <TextBlock Text="No cover"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource FgSubtle}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold"
                                           FontSize="18"
                                           Foreground="{DynamicResource Fg}"/>
                                <TextBlock Text="{Binding SourceDisplayName}"
                                           Margin="0,6,0,0"
                                           FontSize="14"
                                           Foreground="{DynamicResource FgMuted}"/>
                                <TextBlock Text="{Binding AddedDisplay, StringFormat=Added {0}}"
                                           Margin="0,4,0,0"
                                           FontSize="12"
                                           Foreground="{DynamicResource FgSubtle}"/>
                            </StackPanel>

                            <Button Grid.Column="3"
                                    Content="Remove"
                                    Command="{Binding DataContext.RemoveBookmarkCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Padding="14,6"
                                    MinWidth="90"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Style="{StaticResource EmptyStatePanelStyle}"
                        Visibility="{Binding ShowEmptyState, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text=""
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="32"
                           Foreground="{DynamicResource FgSubtle}"
                           Opacity="0.7"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding EmptyStateMessage}"
                           Foreground="{DynamicResource FgMuted}"
                           FontSize="14"
                           Margin="0,12,0,0"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="320"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
<UserControl x:Class="MSCS.Views.AniListRecommendationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:MSCS.Models"
             xmlns:enums="clr-namespace:MSCS.Enums"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             xmlns:conv="clr-namespace:MSCS.Converters"
             Height="Auto" Width="Auto">
    <UserControl.Resources>
        <SolidColorBrush x:Key="Fg" Color="White"/>
        <SolidColorBrush x:Key="Accent" Color="#5865F2"/>

        <SolidColorBrush x:Key="CardBackground" Color="#1A2130"/>
        <SolidColorBrush x:Key="CardBorder" Color="#262F42"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#9AA5B1"/>

        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:PackToArrayConverter x:Key="PackToArrayConverter"/>

        <Style x:Key="AccentButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="Margin" Value="12,0,0,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                                Content="{TemplateBinding Content}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#91ACFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1F2738"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MediaCardButton" TargetType="Button">
            <Setter Property="Background" Value="#222A3A"/>
            <Setter Property="BorderBrush" Value="#2D364A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MediaCardMenuButton" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="0,8,8,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Background" Value="#1F2738"/>
            <Setter Property="BorderBrush" Value="#2D364A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Content" Value="⋯"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="MenuBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MenuBorder" Property="Background" Value="#3B4864"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="MenuBorder" Property="Background" Value="#2A344A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="MenuBorder" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="RecommendationCardTemplate" DataType="{x:Type models:AniListMedia}">
            <Grid Width="220" Margin="0,0,16,16">
                <Button Style="{StaticResource MediaCardButton}"
                        Margin="0"
                        Command="{Binding DataContext.OpenSeriesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        CommandParameter="{Binding}">
                    <Button.ToolTip>
                        <ToolTip Content="{Binding SiteUrl}"/>
                    </Button.ToolTip>
                    <StackPanel>
                        <Border Height="260"
                                Background="#222A3A"
                                CornerRadius="10"
                                Margin="0,0,0,12"
                                ClipToBounds="True">
                            <Image Source="{Binding CoverImageUrl}"
                                   Stretch="UniformToFill"
                                   SnapsToDevicePixels="True"/>
                        </Border>

                        <TextBlock Text="{Binding DisplayTitle}"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource Fg}"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding FormatDisplay}"
                                   Foreground="{StaticResource MutedBrush}"
                                   Margin="0,6,0,0"/>

                        <TextBlock Text="{Binding StartDateText}"
                                   Foreground="{StaticResource MutedBrush}"
                                   Margin="0,2,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </Trigger>
                                        <Trigger Property="Text" Value=" ">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AverageScoreDisplay}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AverageScoreDisplay}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="Score" Foreground="{StaticResource MutedBrush}"/>
                            <TextBlock Text=":" Foreground="{StaticResource MutedBrush}"/>
                            <TextBlock Text="{Binding AverageScoreDisplay}"
                                       Foreground="{StaticResource Fg}"
                                       Margin="4,0,0,0"/>
                        </StackPanel>

                        <TextBlock Text="{Binding UserStatus, StringFormat=Status: {0}}"
                                   Foreground="{StaticResource MutedBrush}"
                                   Margin="0,6,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UserStatus}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <TextBlock Text="Click to view on AniList"
                                   Margin="0,8,0,0"
                                   Foreground="{StaticResource MutedBrush}"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource MediaCardMenuButton}" Click="OnMenuButtonClick">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Mark as Currently Reading"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Current}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <MenuItem Header="Add to Plan to Read"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Planning}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <MenuItem Header="Mark as Completed"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Completed}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <MenuItem Header="Mark as Paused"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Paused}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <MenuItem Header="Mark as Dropped"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Dropped}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <MenuItem Header="Mark as Re-reading"
                                      Command="{Binding ChangeStatusCommand}">
                                <MenuItem.CommandParameter>
                                    <MultiBinding Converter="{StaticResource PackToArrayConverter}">
                                        <Binding Path="PlacementTarget.DataContext"
                                                 RelativeSource="{RelativeSource AncestorType=ContextMenu}"/>
                                        <Binding Source="{x:Static enums:AniListMediaListStatus.Repeating}"/>
                                    </MultiBinding>
                                </MenuItem.CommandParameter>
                            </MenuItem>

                            <Separator/>

                            <MenuItem Header="Open tracking editor..."
                                      Command="{Binding EditTrackingCommand}"
                                      CommandParameter="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </Grid>
        </DataTemplate>
    </UserControl.Resources>


    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="24">
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="AniList Recommendations"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Fg}"/>
                    <Button Grid.Column="1"
                            Content="Refresh"
                            MinWidth="110"
                            Style="{StaticResource AccentButton}"
                            Command="{Binding RefreshCommand}"/>
                </Grid>

                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource MutedBrush}"
                           FontStyle="Italic"
                           Margin="0,0,0,16">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusMessage}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel>
                    <TextBlock Text="Top Manga"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Fg}"
                               Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding TopManga}"
                                  ItemTemplate="{StaticResource RecommendationCardTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                    <Button Content="Show more"
                            Margin="0,8,0,0"
                            Command="{Binding ShowMoreMangaCommand}"
                            Visibility="{Binding CanShowMoreManga, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Style="{StaticResource AccentButton}"/>
                </StackPanel>

                <StackPanel Margin="0,24,0,0">
                    <TextBlock Text="Top Manhwa"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Fg}"
                               Margin="0,0,0,8"/>
                    <ItemsControl ItemsSource="{Binding TopManhwa}"
                                  ItemTemplate="{StaticResource RecommendationCardTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                    <Button Content="Show more"
                            Margin="0,8,0,0"
                            Command="{Binding ShowMoreManhwaCommand}"
                            Visibility="{Binding CanShowMoreManhwa, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Style="{StaticResource AccentButton}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

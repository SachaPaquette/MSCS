<UserControl x:Class="MSCS.Views.ReaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:MSCS.Converters"
             Background="#0F151F"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch">

    <UserControl.Resources>
        <conv:ViewportFractionConverter x:Key="ViewportFraction"/>

        <Style x:Key="IconBtn" TargetType="Button">
            <Setter Property="Foreground" Value="#E9EEF7"/>
            <Setter Property="Background" Value="#00000000"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E2A3E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#25344E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer x:Name="ScrollView"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="#0F151F"
                      ScrollChanged="ScrollViewer_ScrollChanged">
            <ItemsControl ItemsSource="{Binding ImageUrls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="0,8,0,8"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Stretch="Uniform"
                               SnapsToDevicePixels="True"
                               UseLayoutRounding="True"
                               RenderOptions.BitmapScalingMode="HighQuality"
                               MouseLeftButtonDown="Image_MouseLeftButtonDown"
                               Source="{Binding ImageUrl}">
                            <Image.Width>
                                <MultiBinding Converter="{StaticResource ViewportFraction}">
                                    <Binding ElementName="ScrollView" Path="ViewportWidth"/>
                                    <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext.WidthFactor"/>
                                    <Binding RelativeSource="{RelativeSource AncestorType=UserControl}" Path="DataContext.MaxPageWidth"/>
                                </MultiBinding>
                            </Image.Width>
                        </Image>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Grid HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4" Panel.ZIndex="10">
            <StackPanel>
                <Button Style="{StaticResource IconBtn}"
                        Command="{Binding GoBackCommand}"
                        ToolTip="Back"
                        Margin="0,0,0,6">
                    <TextBlock Text="&#xE72B;" FontSize="16"/>
                </Button>

                <Button x:Name="PinBtn" Style="{StaticResource IconBtn}" ToolTip="Chapters / Home">
                    <TextBlock Text="&#xE718;" FontSize="16"/>
                </Button>
            </StackPanel>

            <Popup x:Name="SidePopup"
                   PlacementTarget="{Binding ElementName=PinBtn}"
                   Placement="Right"
                   AllowsTransparency="True"
                   StaysOpen="True">
                <Popup.Style>
                    <Style TargetType="Popup">
                        <Setter Property="IsOpen" Value="False"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, ElementName=PinBtn}" Value="True">
                                <Setter Property="IsOpen" Value="True"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True">
                                <Setter Property="IsOpen" Value="True"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Popup.Style>

                <Border Background="#202737"
                        BorderBrush="#263149"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="10"
                        Width="300"
                        SnapsToDevicePixels="True">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Home -->
                        <Button Grid.Row="0"
                                Style="{StaticResource IconBtn}"
                                Command="{Binding GoHomeCommand}"
                                HorizontalAlignment="Left">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Text="&#xE10F;" Margin="0,0,6,0"/>
                                <!-- home icon -->
                                <TextBlock Text="Home"/>
                            </StackPanel>
                        </Button>

                        <!-- Quick selector -->
                        <ComboBox Grid.Row="2"
                                  ItemsSource="{Binding Chapters}"
                                  SelectedItem="{Binding SelectedChapter, Mode=TwoWay}"
                                  DisplayMemberPath="Title"
                                  HorizontalContentAlignment="Stretch"/>

                        <!-- Full chapter list -->
                        <ListBox Grid.Row="4"
                                 ItemsSource="{Binding Chapters}"
                                 SelectedItem="{Binding SelectedChapter, Mode=TwoWay}"
                                 BorderThickness="0"
                                 Background="#202737"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="#E9EEF7"
                                               Margin="6,4"
                                               TextTrimming="CharacterEllipsis"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>
            </Popup>
        </Grid>
    </Grid>
</UserControl>
